<?xml version="1.0" encoding="utf-8"?>
<!-- Matches the Compose SplashScreen gradient (primary -> primaryDark) so the
     native pre-Compose window background doesn't flash a different color. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="#21888F"
                android:endColor="#166064" />
        </shape>
    </item>
</layer-list>
